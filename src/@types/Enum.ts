export enum SD_Roles {
  Role_Admin = "admin",
  Role_Seller = "seller",
  Role_Buyer = "buyer",
}

export enum EngineType {
  Gasoline = 0,
  Diesel = 1,
  Electric = 2,
  Hybrid = 3,
}

export enum GearType {
  Manual = 0,
  Automatic = 1,
}

export enum CarType {
  FourDoorSedan = 0,
  PickUpTruck = 1,
  CarSUV = 2,
  CarVan = 3,
}

export enum Status {
  Available = 0,
  Sold = 1,
  Reserved = 2,
}

export enum StatusTestDrive {
  Cancel = 0,
  Pending = 1,
  Confirmed = 2,
}